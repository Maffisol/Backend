/*const express = require('express');
const router = express.Router();
const Player = require('../models/player');
const Family = require('../models/family');

// Route voor spelers-leaderboard
/*router.get('/players', async (req, res) => {
    try {
        const players = await Player.find({}, 'username points rank isPro family')
            .sort({ points: -1 }) // Sorteert op punten, hoogste eerst
            .limit(50); // Limiteert het aantal teruggegeven spelers, indien gewenst

        res.json(players);
    } catch (error) {
        console.error('Error fetching player leaderboard:', error);
        res.status(500).json({ message: 'Error fetching player leaderboard' });
    }
});





module.exports = router; */
